start = xml
xml =  element xml {(sp | cutscene)+}
sp = element sp {speaker,mixed{q*}}
speaker = element speaker {text}
cutscene = element cutscene {(desc |sp)+}
desc = element desc {mixed{q*}}
q = element q {text}